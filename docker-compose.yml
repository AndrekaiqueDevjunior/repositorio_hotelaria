services:
  # ============================================================
  # BANCO DE DADOS - PostgreSQL
  # ============================================================
  postgres:
    image: postgres:16-alpine
    container_name: hotel_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${HOTEL_DB_NAME:-hotel_cabo_frio_dev}
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/migrations:/docker-entrypoint-initdb.d
    ports:
      - "${HOTEL_POSTGRES_PORT:-55433}:5432"
    networks:
      - hotel_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    profiles:
      - db-local

  # ============================================================
  # BACKEND - FastAPI + Python
  # ============================================================
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: hotel_backend
    restart: unless-stopped
    # IMPORTANTE: Usar env_file do backend para garantir DATABASE_URL do Prisma remoto
    env_file:
      - ./backend/.env.docker.dev
    environment:
      CORS_ORIGINS: ${HOTEL_CORS_ORIGINS:-http://localhost:3000,http://localhost:18000,http://localhost:18080,http://127.0.0.1:3000,http://127.0.0.1:18000,http://127.0.0.1:18080}
      
    volumes:
      - ./backend:/app
      - backend_cache:/app/.cache
    ports:
      - "${HOTEL_BACKEND_PORT:-18000}:8000"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - hotel_network
    command: >
      sh -c "
        echo 'CRITICO: Usando variaveis de ambiente do container...' &&
        echo 'DATABASE_URL carregada:' &&
        echo $$DATABASE_URL | sed 's/:[^@]*@/:****@/g' &&
        echo 'Gerando Prisma Client com DATABASE_URL correta...' &&
        prisma generate &&
        echo 'Pulando migrations explicitas no dev compose...' &&
        echo 'Iniciando servidor...' &&
        uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
      "

  # ============================================================
  # FRONTEND - Next.js
  # ============================================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: hotel_frontend
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_URL: "/api/v1"
      BACKEND_URL: http://backend:8000
      NODE_ENV: development
      HOSTNAME: "0.0.0.0"
      WATCHPACK_POLLING: "true"
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    ports:
      - "${HOTEL_FRONTEND_PORT:-3000}:3000"
    depends_on:
      - backend
    networks:
      - hotel_network
    # Aumentar memória disponível
    deploy:
      resources:
        limits:
          memory: 2G
    command: npm run dev

  # ============================================================
  # PGADMIN - Gerenciamento do Banco (Opcional)
  # ============================================================
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: hotel_pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@hotel.com
      PGADMIN_DEFAULT_PASSWORD: admin123
      PGADMIN_CONFIG_SERVER_MODE: 'False'
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - hotel_network
    profiles:
      - tools

  # ============================================================
  # NGINX - Proxy Reverso (Unifica Frontend + Backend)
  # ============================================================
  nginx-proxy:
    image: nginx:alpine
    container_name: hotel_nginx_proxy
    restart: unless-stopped
    volumes:
      - ./nginx-proxy.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "${HOTEL_PROXY_PORT:-18080}:80"
    depends_on:
      - backend
      - frontend
    networks:
      - hotel_network
    profiles:
      - ngrok

  # ============================================================
  # NGROK - Túnel para Acesso Externo (Frontend + Backend)
  # ============================================================
  ngrok:
    image: ngrok/ngrok:latest
    container_name: hotel_ngrok
    restart: unless-stopped
    command:
      - "http"
      - "--domain=${NGROK_DOMAIN}"
      - "nginx-proxy:80"
    ports:
      - "4040:4040"  # Dashboard ngrok
    depends_on:
      - nginx-proxy
    networks:
      - hotel_network
    profiles:
      - ngrok
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN:-}

  # ============================================================
  # REDIS - Cache
  # ============================================================
  redis:
    image: redis:7-alpine
    container_name: hotel_redis
    restart: unless-stopped
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    networks:
      - hotel_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

# ============================================================
# VOLUMES
# ============================================================
volumes:
  postgres_data:
    driver: local
  pgadmin_data:
    driver: local
  backend_cache:
    driver: local
  redis_data:
    driver: local

# ============================================================
# NETWORKS
# ============================================================
networks:
  hotel_network:
    driver: bridge
