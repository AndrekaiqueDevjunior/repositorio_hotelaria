# ============================================================
# CONFIGURACAO DE TESTES DE INTEGRACAO - EXEMPLO
# ============================================================
# Copie para .env.test e ajuste os valores

# Base URL do backend (via nginx no Docker)
BASE_URL=http://localhost:8080

# Credenciais de autenticacao
AUTH_EMAIL=admin@hotelreal.com.br
AUTH_PASSWORD=admin123

# Timeout para requests (segundos)
REQUEST_TIMEOUT=30

# Retry configuration
MAX_RETRIES=3
RETRY_BACKOFF=1

# Database (para cleanup se necessario)
DATABASE_URL=postgres://user:password@host:5432/database
